const { User } = require('../models');

const UserController = {
    // [POST] /user/register
    register: (req, res) => {

    },

    // [POST] /user/login
    login: (req, res) => {

    },

    // [GET] /user/logout
    logout: (req, res) => {

    },
};

module.exports = UserController;
